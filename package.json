{
  "name": "simple-server",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "docker-dev": "docker-compose build server && docker-compose run --service-ports server",
    "docker-test": "docker-compose -f docker-compose-test.yml build test && docker-compose -f docker-compose-test.yml",
    "test": "jest test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "jest": "^23.6.0"
  }
}
